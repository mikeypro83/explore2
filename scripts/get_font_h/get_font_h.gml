/////////////////////////////////////////////////////////////////////////////////////////////////
/// @description get_font_h(Font)
/// @param Font Font to calc height for.
/////////////////////////////////////////////////////////////////////////////////////////////////
/////////////////////////////////////////////////////////////////////////////////////////////////
{
	f = draw_get_font();
	font = noone;
	if(argument_count==1)
		font = argument[0];
	else
		font = f;	
	if(f!=font) draw_set_font(font);
	_h = string_height("0QDT#!");
	if(f!=font) draw_set_font(f);
	return _h;
}